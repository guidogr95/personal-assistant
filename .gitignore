# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Environment — never commit real secrets
.env

# Google OAuth token (written at runtime)
google_token.json

# Editor
.idea/
.vscode/

# Data volumes (local dev)
data/
notes/
# Source package — never ignore (the notes/ above only matches root-level dir,
# but git applies gitignore patterns at any depth; force-include the package)
!/src/**/notes/
